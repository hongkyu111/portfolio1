extends ../base

block content
    if errorMessage 
        span=errorMessage 
    form(method="POST") 
        input(placeholder="사용하던 비밀번호", name="oldPassword", type="password")
        input(placeholder="새로운 비밀번호", name="newPassword", type="password")
        input(placeholder="새로운 비밀번호 확인", name="newPassword1", type="password")
        input(value="비밀번호 변경", type="submit")